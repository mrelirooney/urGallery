// frontend/src/lib/search/mock.ts

// Very simple placeholder mock search.
// You can replace this later with a real implementation.
export async function mockSearch(..._args: any[]): Promise<unknown[]> {
  // Return an empty result set for now
  return [];
}
